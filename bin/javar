#!/bin/bash
#java run files
#TODO: make it work with packages
[ -f $1  ] && [ ! -z $(grep .java$ <<< $1) ] && javac -d out/ ./*.java && java -cp ./out/ $(basename -- $1 .java) "${@:2}" || echo file not found...
